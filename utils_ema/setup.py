from setuptools import setup, find_packages
 
setup(  name = "utils_ema",
        # packages=find_packages(include=['utils_ema','utils_ema.*']),
        packages=find_packages(),

        version="1.0")
